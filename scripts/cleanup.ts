/**
 * Scheduled cleanup untuk file sementara & storage.
 * Akan diimplementasikan pada fase processing pipeline (Phase 4+).
 *
 * Jalankan: pnpm dlx tsx scripts/cleanup.ts
 */

console.log('[cleanup] Belum diimplementasikan — menunggu processing pipeline (Phase 4+).');
