/** Masa berlaku signed URL download (detik) — docs/07: expiresIn 300. */
export const DOWNLOAD_URL_EXPIRES_IN_SECONDS = 300;

/** Klaim purpose pada token download agar JWT sesi tidak bisa dipakai mengunduh. */
export const DOWNLOAD_TOKEN_PURPOSE = 'download';
